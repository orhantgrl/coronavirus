name: coronavirus
description: A new way to track corona virus

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+4
  url_launcher: ^5.4.2
  cupertino_icons: ^0.1.2
  charts_flutter: ^0.9.0
  flutter_svg: ^0.17.3+1
  font_awesome_flutter: ^8.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - graphics/